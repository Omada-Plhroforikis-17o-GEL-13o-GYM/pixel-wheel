# Not definitive, but for now
# Renderer - static class
# EngineProperties - static class
# SceneManager - static class

# for engine simplicity sake, but you lose on the scripts written outside of the engine need to account for more.

# 2.2-alpha must focus on making simpler for the client of the engine, not the engine itself (that would be preferable, but who cares)

# for that simplicity we need to develop more the ECS in tleng2

# from .engine import GameEngine, GameEngine_exp


# engine_exp = GameEngine_exp()